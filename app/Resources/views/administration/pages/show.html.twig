{% extends ':layout:layoutAdmin.html.twig' %}

{% block body %}
    <div class="span9">
    <h1>Page</h1>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Titre</th>
                <td>{{ page.titre }}</td>
            </tr>
            <tr>
                <th>Contenu</th>
                <td>{{ page.contenu }}</td>
            </tr>
        </tbody>
    </table>



            <a href="{{ path('adminPages_edit', { 'id': page.id }) }}" class="btn btn-primary">Edit</a>

            {{ form_start(delete_form) }}
                <input id="cat-del" type="submit" class="btn btn-primary" value="Delete">
            {{ form_end(delete_form) }}
    <hr>
    <a href="{{ path('adminPages_index') }}" class="btn btn-primary"><i class="icon-arrow-left"></i> Retour</a>
    </div>
{% endblock %}
