{% extends 'layout/layoutAdmin.html.twig' %}

{% block body %}
    <div class="span9">
        <h2>Page creation</h2>
        <hr>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <br/><input type="submit" class="btn btn-primary" value="Créer"/>
        {{ form_end(form) }}
        <hr>
        <a href="{{ path('adminPages_index') }}" class="btn btn-primary"><i class="icon-arrow-left"></i> Retour</a>
    </div>
{% endblock %}
