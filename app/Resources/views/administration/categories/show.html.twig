{% extends 'layout/layoutAdmin.html.twig' %}

{% block body %}
    <iv class="span6">
    <h1>Catégorie</h1>
    <hr>

    <table class="table table-striped table-hover">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ category.nom }}</td>
            </tr>
        </tbody>
    </table>


<br/>
        <a href="{{ path('categories_edit', { 'id': category.id }) }}" class="btn btn-primary"><i class="icon-edit"></i> Edit</a>
        {{ form_start(delete_form) }}
         <input id="cat-del" type="submit" class="btn btn-primary" value="Effacer">
        {{ form_end(delete_form) }}
        <hr>

        <a href="{{ path('categories_index') }}" class="btn btn-primary"><i class="icon-arrow-left"></i> Retour</a>

        </div>
{% endblock %}
