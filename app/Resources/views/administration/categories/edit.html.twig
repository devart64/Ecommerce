{% extends 'layout/layoutAdmin.html.twig' %}

{% block body %}
    <div class="span9">
        <h1>Editer la catégorie</h1>
        <hr>
        {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" class="btn btn-primary" value="Edit"/>
        {{ form_end(edit_form) }}

        {{ form_start(delete_form) }}
        <input id="cat-del" type="submit" class="btn btn-primary" value="Delete">
        {{ form_end(delete_form) }}
        <hr>
        <a href="{{ path('categories_index') }}" class="btn btn-primary"><i class="icon-arrow-left"></i> Retour</a>
    </div>
{% endblock %}
